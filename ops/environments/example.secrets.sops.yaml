# This file is documentation only.  The ENC[...] values are deliberately
# unusable samples, not SOPS ciphertext and not production credentials.
database_password: ENC[not-a-real-sops-value]
secret_key_base: ENC[not-a-real-sops-value]
ash_authentication_token_signing_secret: ENC[not-a-real-sops-value]
resend_api_key: ENC[not-a-real-sops-value]
