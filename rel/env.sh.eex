#!/bin/sh

# Keep the production node reachable only from this host for break-glass
# inspection through `bin/taskman remote` or `bin/taskman rpc`.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@127.0.0.1
